# Environment Variables for Moi Dither Build Process

# Development
NODE_ENV=development

# Code Signing (Optional but recommended for production releases)
# Windows
# CSC_LINK=path/to/certificate.pfx
# CSC_KEY_PASSWORD=your_certificate_password

# macOS
# APPLE_ID=your@apple.id
# APPLE_ID_PASSWORD=your-app-specific-password
# APPLE_TEAM_ID=your_team_id

# GitHub Release (if using personal access token instead of GITHUB_TOKEN)
# GH_TOKEN=your_github_personal_access_token

# Auto-updater Configuration
# These are set automatically by electron-updater from app-update.yml
# ELECTRON_UPDATER_PROVIDER=github
# ELECTRON_UPDATER_OWNER=moikas-code
# ELECTRON_UPDATER_REPO=moikas-ditherer

# Build Configuration
# ELECTRON_CACHE=path/to/electron/cache
# ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/

# Notes:
# 1. For production builds, always set NODE_ENV=production
# 2. Code signing is highly recommended to avoid security warnings
# 3. The auto-updater will work without code signing but users will see warnings
# 4. GitHub Actions automatically provides GITHUB_TOKEN for releases